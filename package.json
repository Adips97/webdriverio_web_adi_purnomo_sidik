{
  "name": "webdriverio_web_adi_purnomo_sidik",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "wdio": "wdio run ./wdio.conf.js",
    "clean:allure": "npx rimraf allure-results",
    "allure:generate": "npx allure generate allure-results --clean",
    "allure:open": "npx allure open allure-report",
    "prepare:allure": "npm run clean:allure && npm run wdio && npm run allure:generate && npm run allure:open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@wdio/allure-reporter": "^9.1.2",
    "@wdio/cli": "^9.1.2",
    "@wdio/cucumber-framework": "^9.1.2",
    "@wdio/local-runner": "^9.1.2",
    "@wdio/spec-reporter": "^9.1.2",
    "allure-commandline": "^2.30.0",
    "rimraf": "^6.0.1"
  }
}
